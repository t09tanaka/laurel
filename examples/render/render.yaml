# Copy this file to the root of a Nectar site repository before creating a
# Render Blueprint.

services:
  - type: static
    name: nectar-site
    buildCommand: bun install && bun run build
    publishPath: ./dist
    envVars:
      - key: BUN_VERSION
        value: "1.3.0"
