# Copy this file to the root of a Laurel site repository before creating a
# Render Blueprint.

services:
  - type: static
    name: laurel-site
    buildCommand: bun install && bun run build
    publishPath: ./dist
    envVars:
      - key: BUN_VERSION
        value: "1.3.0"
