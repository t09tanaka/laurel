{{!< default}}
<section class="solo-author">
  <h1>{{author.name}}</h1>
  {{#foreach posts}}
    <article class="{{post_class}}">
      <h2><a href="{{url}}">{{title}}</a></h2>
    </article>
  {{/foreach}}
</section>
