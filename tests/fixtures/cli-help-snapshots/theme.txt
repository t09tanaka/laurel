Manage themes in the project. `new <name>` scaffolds a minimal theme; `zip` packs the active theme into a `<name>-<version>.zip` archive; `lint <path>` checks a theme directory for required templates / helpers / partials

Usage:
  nectar theme [--config <path>] [--from <theme-name>] [--output <path>] [--force] [--json] <subcommand...>

Arguments:
  subcommand          `new <name>` (scaffold themes/<name>/), `zip` (archive the active theme into a gscan-compatible .zip), or `lint <path>` (audit a theme directory)

Options:
  -c, --config <path> Path to a config file; disables discovery when set
  --from <theme-name> `new` only: copy from an existing theme directory under `themes/` instead of writing the minimal default scaffold
  -o, --output <path> `zip` only: output path for the archive (defaults to `<name>-<version>.zip` in the current directory)
  --force             Overwrite the destination directory (`new`) or archive (`zip`) if it already exists
  -j, --json          `lint` only: emit findings as JSON instead of the default table
  -h, --help          Show help for this command

Environment variables:
  Every flag has an env var fallback (CLI flag > env var > config > default).
  Naming: NECTAR_<COMMAND>_<FLAG> (uppercased, dashes become underscores).
  Example: --config → NECTAR_THEME_CONFIG

Examples:
  nectar theme new my-theme                    # scaffold themes/my-theme/
  nectar theme new my-fork --from source       # fork the active theme
  nectar theme zip                             # ship-ready zip in cwd
  nectar theme lint themes/my-theme            # audit before shipping
  nectar theme:lint themes/my-theme            # colon-style alias
