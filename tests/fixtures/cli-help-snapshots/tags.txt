Inspect or modify tags in the project

Usage:
  laurel tags [--config <path>] [--orphaned] [--unused] [--json] [--dry-run] <subcommand...>

Arguments:
  subcommand          `list` (show tags), `rename <old-slug> <new-slug>`, or `merge <from> [from...] <into>` (rewrite post/page tag references and safely handle tag files)

End of options:
  Use `--` before positional values that start with `-`, for example `laurel new post -- --config`.

Options:
  -c, --config <path> Config path(s); repeat or comma-separate to deep-merge in order
  --orphaned          Show only tags that are defined under content/tags/ but referenced by zero posts (`list` only)
  --unused            Alias for --orphaned (`list` only)
  -j, --json          Emit results as JSON for CI consumption (`list`, `rename`, and `merge`)
  --dry-run           On `rename`/`merge`: scan and report the files that would change without writing anything
  -h, --help          Show help for this command

Environment variables:
  Every flag has an env var fallback (CLI flag > env var > .laurelrc > config > default).
  Naming: LAUREL_<COMMAND>_<FLAG> (uppercased, dashes become underscores).
  Example: --config → LAUREL_TAGS_CONFIG

Repeated flags:
  Scalar string flags use the last value.
  List-style string flags accumulate as comma-separated values in argv order.
  Boolean flags use the last positive or negated spelling.

Examples:
  laurel tags list                             # all tags + post counts
  laurel tags list --orphaned                  # tags defined but unused
  laurel tags rename old-tag new-tag
  laurel tags rename old new --dry-run         # preview files that would change
  laurel tags merge draft old canonical --dry-run
