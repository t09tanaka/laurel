<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom">
<channel>
<title>Nectar Example</title>
<link>https://nectar.example.com</link>
<description>A demo blog built with Nectar against the Ghost Source theme</description>
<language>en</language>
<lastBuildDate>Tue, 19 May 2026 09:00:00 GMT</lastBuildDate>
<atom:link href="https://nectar.example.com/rss.xml" rel="self" type="application/rss+xml"/>
<image><url>https://nectar.example.com/content/images/logo.svg</url><title>Nectar Example</title><link>https://nectar.example.com</link></image>
<item><title>Hello, Nectar</title><link>https://nectar.example.com/hello-nectar/</link><guid isPermaLink="false">post-hello-nectar</guid><pubDate>Tue, 19 May 2026 09:00:00 GMT</pubDate><description>First post! A quick tour of what&apos;s working and what&apos;s coming next.</description><content:encoded><![CDATA[<p>Welcome to <strong>Nectar</strong> — a static site generator built around Ghost themes and
Markdown content.</p>
<h2 id="what-works-today">What works today</h2>
<ul>
<li>The whole Ghost Source theme renders end-to-end against this content.</li>
<li>Posts, pages, tags, authors, pagination, RSS, sitemap — all generated.</li>
<li>Theme custom settings (<code>@custom.*</code>) come from <code>nectar.toml</code>.</li>
</ul>
<h2 id="what-were-working-on-next">What we're working on next</h2>
<ul>
<li>Smarter <code>{{img_url}}</code> with build-time resizing.</li>
<li>A <code>nectar dev</code> server that watches content and theme files.</li>
<li>A pluggable component registry for search, comments, and OG image generation.</li>
</ul>
<blockquote>
<p>Nectar is the substance bees turn into honey. We thought it was a fitting
name for the substrate a Ghost theme turns into a blog.</p>
</blockquote>
]]></content:encoded></item><item><title>Migrating from Ghost</title><link>https://nectar.example.com/migrating-from-ghost/</link><guid isPermaLink="false">post-migrating-from-ghost</guid><pubDate>Fri, 15 May 2026 11:30:00 GMT</pubDate><description>How `nectar import-ghost` turns a Ghost JSON export into Markdown.</description><content:encoded><![CDATA[<p>Run <code>nectar import-ghost ghost-export.json</code> from your project root. The
importer reads your Ghost admin export, converts each post's HTML body to
Markdown via <a href="https://github.com/mixmark-io/turndown">turndown</a>, and writes:</p>
<ul>
<li><code>content/posts/*.md</code> (and <code>content/pages/*.md</code> for static pages)</li>
<li><code>content/tags/*.md</code> for tags with descriptions or images</li>
<li><code>content/authors/*.md</code></li>
</ul>
<h2 id="what-about-images">What about images?</h2>
<p>Image URLs are kept as-is. If your Ghost site served them from
<code>/content/images/</code>, copy the <code>content/images/</code> directory from your Ghost
backup into your Nectar project, and the URLs will continue to resolve.</p>
<h2 id="frontmatter-mapping">Frontmatter mapping</h2>
<table>
<thead>
<tr>
<th>Ghost field</th>
<th>Nectar frontmatter</th>
</tr>
</thead>
<tbody><tr>
<td><code>published_at</code></td>
<td><code>date</code></td>
</tr>
<tr>
<td><code>feature_image</code></td>
<td><code>feature_image</code></td>
</tr>
<tr>
<td><code>tags</code></td>
<td><code>tags: [slug, ...]</code></td>
</tr>
<tr>
<td><code>authors</code></td>
<td><code>authors: [slug, ...]</code></td>
</tr>
<tr>
<td><code>visibility</code></td>
<td><code>visibility</code></td>
</tr>
</tbody></table>
<p>Anything Ghost-specific that doesn't map cleanly (Mobiledoc/Lexical-only
posts) is logged so you can revisit it after the import.</p>
]]></content:encoded></item><item><title>Writing with Markdown</title><link>https://nectar.example.com/writing-with-markdown/</link><guid isPermaLink="false">post-writing-with-markdown</guid><pubDate>Sun, 10 May 2026 15:00:00 GMT</pubDate><description>A short primer on the frontmatter conventions Nectar expects.</description><content:encoded><![CDATA[<p>Posts are Markdown files with YAML frontmatter. The frontmatter is the same
shape Ghost uses to talk to themes, just expressed in YAML instead of JSON.</p>
<pre><code class="language-yaml">---
title: "My Post"
slug: my-post
date: 2026-01-01T09:00:00Z
authors: [casper]
tags: [news]
feature_image: /content/images/cover.jpg
custom_excerpt: "Optional summary"
status: published
---
</code></pre>
<h2 id="the-body">The body</h2>
<p>The body is plain Markdown, with GFM tables, code fences, and heading IDs.</p>
<pre><code class="language-js">function hello(name) {
  return `hello, ${name}`;
}
</code></pre>
<p>Inline <code>code</code>, <strong>bold</strong>, <em>italic</em>, strikethrough, footnotes[^1], and
ordinary lists:</p>
<ol>
<li>Posts go in <code>content/posts/</code>.</li>
<li>Pages go in <code>content/pages/</code>.</li>
<li>Drafts (<code>status: draft</code>) are skipped at build time.</li>
</ol>
<p>[^1]: Yes, footnotes too.</p>
]]></content:encoded></item><item><title>Ghost Theme Compatibility</title><link>https://nectar.example.com/ghost-theme-compatibility/</link><guid isPermaLink="false">post-ghost-theme-compatibility</guid><pubDate>Tue, 05 May 2026 08:00:00 GMT</pubDate><description>Which Ghost helpers Nectar implements, and which it deliberately doesn&apos;t.</description><content:encoded><![CDATA[<p>Nectar implements the Ghost theme helper surface needed to render real-world
themes against static Markdown content. The full coverage matrix lives in
<code>docs/GHOST_COMPATIBILITY.md</code> — here are the highlights.</p>
<h2 id="implemented">Implemented</h2>
<p><code>asset</code>, <code>img_url</code>, <code>ghost_head</code>, <code>ghost_foot</code>, <code>date</code>, <code>t</code>, <code>url</code>, <code>concat</code>,
<code>encode</code>, <code>link</code>, <code>link_class</code>, <code>navigation</code>, <code>pagination</code>, <code>reading_time</code>,
<code>excerpt</code>, <code>content</code>, <code>authors</code>, <code>tags</code>, <code>social_url</code>, <code>lang</code>, <code>foreach</code>,
<code>is</code>, <code>match</code>, <code>has</code>, and a <code>get</code> resolver against the local content graph.</p>
<h2 id="deliberately-scoped-out">Deliberately scoped out</h2>
<p>Members, subscriptions, paywall, comments. <code>{{comments}}</code> outputs an empty
container; the optional components system is where you'd wire Giscus / Disqus
in.</p>
<h2 id="currently-best-effort">Currently best-effort</h2>
<p><code>{{img_url size=...}}</code> doesn't transcode images yet — the size parameter is
recognised, but the same source URL is returned. We'll add real resizing as
an optional <code>[components.images]</code> component.</p>
]]></content:encoded></item><item><title>Optional Components</title><link>https://nectar.example.com/optional-components/</link><guid isPermaLink="false">post-optional-components</guid><pubDate>Tue, 28 Apr 2026 12:00:00 GMT</pubDate><description>Search, RSS, sitemap, comments — opt in by config.</description><content:encoded><![CDATA[<p>The Nectar core is intentionally small: load content, load theme, render,
emit. Everything else is an <strong>optional component</strong>, configured in <code>nectar.toml</code>.</p>
<pre><code class="language-toml">[components.search]
enabled = true
type    = "pagefind"

[components.rss]
enabled = true
items   = 20

[components.sitemap]
enabled = true

[components.comments]
provider = "giscus"
repo     = "myorg/myblog-comments"
</code></pre>
<p>Each component is a TypeScript module that hooks into the build pipeline
after route emission. It can add files (a <code>pagefind/</code> directory), rewrite
HTML (inject a Giscus snippet under <code>{{comments}}</code>), or both.</p>
<p>Components ship in the same repo as Nectar but are off by default — if you
don't enable it, no code runs.</p>
]]></content:encoded></item><item><title>Static-Only Runtime</title><link>https://nectar.example.com/static-runtime/</link><guid isPermaLink="false">post-static-runtime</guid><pubDate>Mon, 20 Apr 2026 14:00:00 GMT</pubDate><description>Why Nectar emits plain files and nothing else.</description><content:encoded><![CDATA[<p>A Nectar build produces a tree of plain HTML, CSS, JS, and image assets in
<code>dist/</code>. There is no Node server, no edge function, no database. You can:</p>
<ul>
<li>Drop <code>dist/</code> into S3 or R2 behind any CDN.</li>
<li>Push it to GitHub Pages or Cloudflare Pages.</li>
<li><code>rsync</code> it to a single VPS and serve it with nginx.</li>
</ul>
<p>Because the output is static, anything that <em>requires</em> a server (members
paywalls, server-side search, comment storage) lives at the edges as optional
client-side components — never in the core renderer. That keeps the security
surface and the deploy story boring, which is exactly what a blog needs.</p>
]]></content:encoded></item><item><title>Markdown Meets Git</title><link>https://nectar.example.com/markdown-meets-git/</link><guid isPermaLink="false">post-markdown-meets-git</guid><pubDate>Sun, 12 Apr 2026 07:00:00 GMT</pubDate><description>How treating content as Markdown in Git changes editorial workflow.</description><content:encoded><![CDATA[<p>Treating content as Markdown files in a Git repo unlocks workflow primitives
that a database-backed CMS struggles to express:</p>
<ul>
<li><strong>Branch a draft.</strong> <code>git switch -c drafts/holiday-roundup</code>, write in your
editor, push, get a pull request review from your team, merge to publish.</li>
<li><strong>Schedule a post by merging on a date.</strong> The build pipeline runs on push,
and <code>status: draft</code> keeps things hidden until you flip it.</li>
<li><strong>History is free.</strong> <code>git log content/posts/&lt;slug&gt;.md</code> is the version
history. No vendor lock-in.</li>
</ul>
<p>Content as code, code as content. Either way, you get diffs, reviews, and
deploys for free.</p>
]]></content:encoded></item>
</channel>
</rss>